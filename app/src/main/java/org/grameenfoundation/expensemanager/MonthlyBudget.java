package org.grameenfoundation.expensemanager;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.CalendarView;
import android.widget.DatePicker;

public class MonthlyBudget extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.monthly_budget);
        //intializeCalendar();
//        CalendarView mCalendar = (CalendarView)findViewById(R.id.calendarView);

//        DatePicker mPicker = (DatePicker)findViewById(R.id.datePicker);

        
    }

    private void intializeCalendar() {

       // CalendarView mCalendar = (CalendarView)findViewById(R.id.calendarView);
        //mCalendar

    }
}
